---
title: "Background Blend Mode (背景混合模式)"
# description: "Utilities for controlling how an element's background image should blend with its background color."
description: "控制將元素的背景圖片跟背景顏色混合的功能"
featureVersion: 'v2.1+'
---

import plugin from 'tailwindcss/lib/plugins/backgroundBlendMode'
import { Variants } from '@/components/Variants'
import { Disabling } from '@/components/Disabling'
import Responsive from '@/components/Responsive.mdx'

export const classes = { plugin }

## Usage

<!-- Use the `bg-blend-{mode}` utilities to control how an element's background image should be blended its background color. -->
使用 `bg-blend-{mode}` 功能來控制元素的背景照片跟背景顏色要怎麼混合。

```html
<div class="bg-blend-multiply ...">
  <!-- ... -->
</div>
```

## Responsive

<!-- To control the background-blend-mode property at a specific breakpoint, add a `{screen}:` prefix to any existing background-blend-mode utility. For example, use `md:bg-blend-darken` to apply the `bg-blend-darken` utility at only medium screen sizes and above. -->
如要讓元素的背景混合模式有響應式效果，可在現有背景混合模式功能前加上前綴詞 `{screen}:` 。例如：中型或以上的螢幕尺寸使用 `md:bg-blend-darken` 以套用 `bg-blend-darken` 功能。

```html
<div class="bg-blend-lighten **md:bg-blend-darken** ...">
  <!-- ... -->
</div>
```

<Responsive />

## Customizing

### Variant

<Variants plugin="backgroundBlendMode" name="background-blend-mode" />

### Disabling

<Disabling plugin="backgroundBlendMode" name="background-blend-mode" />
